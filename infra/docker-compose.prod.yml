# Optional second file for docker-compose -f ... -f ...
# Ports: set POSTGRES_HOST_PORT and REDIS_HOST_PORT in .env (see infra/deploy/env.production.example)

services:
  postgres:
    restart: unless-stopped
  redis:
    restart: unless-stopped
